// exercicio-
// Qual o resultado da seguinte expressão?
var total = 10 + 5 * 2 / 2 + 20;
35

// Crie duas expressões que retornem NaN
var idade = 'Ele tem 10 anos e seu amigo tem o dobro';
var oDobro = idade * 2;

// minha mulher quer o dobro de carne que o irmão dela comprou para o churrasco 
var compras = '5kg de carne';
var comprasD = compras * 2;

// Somar a string '200' com o número 50 e retornar 250
var string = +'200';
var soma = string + 50;

// Incremente o número 5 e retorne o seu valor incrementado
var n = 5;
console.log(++n)
 
// Como dividir o peso por 2?
var numero = +'80';
var unidade = 'kg';
var peso = numero + unidade; // '80kg'
var pesoPorDois = peso / 2; // NaN (Not a Number)

var total = numero / 2 + unidade;